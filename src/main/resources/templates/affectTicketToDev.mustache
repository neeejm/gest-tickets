{{>layout/header}}
<h1 class="h3 mb-2 text-gray-800">Affect Ticket to Dev</h1>
<form action="/admin/affect" method="POST">
  <div class="form-group">
    <label for="tickets">Tickets:</label>
    <select class="form-control" id="tickets" name="ticketId">
    {{#tickets}}
        <option value="{{id}}">{{description}}</option>
    {{/tickets}}
    </select>
  </div>
  <div class="form-group">
    <label for="tickets">Tickets:</label>
    <select class="form-control" id="devs" name="devId">
    {{#devs}}
        <option value="{{id}}">{{displayName}}</option>
    {{/devs}}
    </select>
  </div>
  <input type="submit" class="btn btn-primary" value="affect" />
</form>
{{>layout/footer}}